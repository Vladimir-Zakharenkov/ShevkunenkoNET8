@model PageInfoModel

@if (!string.IsNullOrEmpty(Model.PageHeading))
{
    <h1 class="fs-3 mt-2 text_shadow">@Html.Raw(Model.PageHeading)</h1>
}

@if (Model.ImagePageHeadingId != null)
{
    <hr class="liniya" />

    <vc:view-image image-id="@Model.ImagePageHeadingId.ToString()"
                   css-class="adp90_img maxwidth700px ten"
                   icon-type="" />
}